# Используем официальный образ для вашего приложения из Docker Hub
FROM alesban/hiclass-api:latest

# Устанавливаем рабочую директорию в контейнере
WORKDIR /app

# Открываем порт для вашего приложения
EXPOSE 80
